[Unit]
Description=ttyd
Before=hostname_capture.service

[Service]
EnvironmentFile=/etc/ttyd_env
ExecStart=/bin/ttyd -W -t titleFixed=\'${HOSTNAME}\' /bin/login-patched
Restart=always
StartLimitInterval=0

[Install]
WantedBy=multi-user.target
